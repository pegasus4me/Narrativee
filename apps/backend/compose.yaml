services:
  ts-app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    init: true
    # env_file: ./.env  # Uncomment if .env file exists
    # Only expose ports if specified in documentation or Dockerfile
    # Add depends_on if external services are added below
    # networks:
    #   - appnet
    # Add comments for any non-obvious configuration

# No external services detected from the provided context.
# If your app requires a database or other services, add them below.

# networks:
#   appnet:
#     driver: bridge
